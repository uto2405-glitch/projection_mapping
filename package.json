{
  "name": "live-drawing-projection",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --strictPort --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "relay": "node relay/relay.mjs",
    "field": "vite build && vite preview --host --strictPort --port 5173"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "three": "^0.185.1",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "pngjs": "^7.0.0",
    "vite": "^8.1.5"
  }
}
