{
  "name": "live-drawing-projection",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --strictPort --port 5173",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
